apiVersion: v1
kind: Service
metadata:
  name: {{ include "creatium-service.fullname" . }}
  labels:
    {{- include "creatium-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "creatium-service.selectorLabels" . | nindent 4 }}
