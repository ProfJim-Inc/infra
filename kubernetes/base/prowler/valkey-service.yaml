apiVersion: v1
kind: Service
metadata:
  name: prowler-valkey
  namespace: production
  labels:
    app.kubernetes.io/name: prowler-valkey
    app.kubernetes.io/part-of: prowler
    app.kubernetes.io/component: cache
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: valkey
      protocol: TCP
      name: valkey
  selector:
    app.kubernetes.io/name: prowler-valkey
